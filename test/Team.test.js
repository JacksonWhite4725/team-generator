const Team = require('../scripts/Team');

describe('MemberName', () => {
    it('Team members names must be a one word string with the first letter capitlized', () => {
        //expect(no white spaces, value is string, and only first index to be a capital letter)
        expect()
    })
});

describe('Member')